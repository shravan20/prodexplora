{
  "name": "prodexplora",
  "version": "1.0.0",
  "description": "prodexplora - Connecting two dimensions",
  "main": "\"\"",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install",
    "dev-setup": "npm i && npm run prepare && cd server && npm install && cd .. && cd ui && npm install"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "husky": "^8.0.0"
  }
}
